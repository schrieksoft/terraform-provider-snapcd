---
page_title: "snapcd_runner_module_assignment Resource - snapcd"
subcategory: "Runners"
description: |-
  Manages a Runner Module Assignment in Snap CD.
---

# snapcd_runner_module_assignment (Resource)

Manages a Runner Module Assignment in Snap CD.


## Example Usage

```terraform
data "snapcd_stack" "default" {
  name = "default"
}

resource "snapcd_namespace" "mynamespace" {
  name     = "mynamespace"
  stack_id = data.snapcd_stack.default.id
}

resource "snapcd_runner" "myrunnerpool" {
  name = "myrunnerpool"
}

resource "snapcd_module" "mymodule" {
  name                = "mymodule"
  namespace_id        = snapcd_namespace.mynamespace.id
  source_revision     = "main"
  source_url          = "https://github.com/schrieksoft/snapcd-samples.git"
  source_subdirectory = "getting-started/two-module-dag/module2"
  runner_id           = data.snapcd_runner.default.id
}


resource "snapcd_runner_module_assignment" "myrunnerpool_mymodule" {
  runner_id = snapcd_runner.myrunnerpool.id
  module_id = snapcd_module.mymodule.id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `module_id` (String) ID of the Module to which the Runner is assigned.
- `runner_id` (String) ID of the Runner that is assigned to the Module.

### Read-Only

- `id` (String) Unique ID of the Runner Module Assignment.

## Import

Import is supported using the following syntax:

```shell
RESOURCE_ID="12345678-90ab-cdef-1234-56789abcdef0"
terraform import snapcd_runner_module_assignment.this $RESOURCE_ID
```
