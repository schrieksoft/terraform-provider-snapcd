---
page_title: "snapcd_module_hook Resource - snapcd"
subcategory: "Hooks"
description: |-
  Manages a Module Hook in Snap CD. A hook is a shell script that runs before or after a Terraform/Pulumi lifecycle task on a specific Module.
---

# snapcd_module_hook (Resource)

Manages a Module Hook in Snap CD. A hook is a shell script that runs before or after a Terraform/Pulumi lifecycle task on a specific Module.


## Example Usage

```terraform
resource "snapcd_module_hook" "before_plan" {
  module_id = snapcd_module.mymodule.id
  task      = "Plan"
  phase     = "Before"
  script    = "echo 'about to plan'"
}

resource "snapcd_module_hook" "after_apply" {
  module_id = snapcd_module.mymodule.id
  task      = "Apply"
  phase     = "After"
  script    = "echo 'apply finished'"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `module_id` (String) ID of the parent Module.
- `phase` (String) When the hook runs relative to the task. Valid values: `Before`, `After`.
- `script` (String) The shell script that runs at the configured task and phase.
- `task` (String) The lifecycle task this hook applies to. Valid values: `Init`, `Plan`, `PlanDestroy`, `Apply`, `Destroy`, `Output`, `Validate`.

### Read-Only

- `id` (String) Unique ID of the Module Hook.

## Import

Import is supported using the following syntax:

```shell
terraform import snapcd_module_hook.before_plan "00000000-0000-0000-0000-000000000000"
```
